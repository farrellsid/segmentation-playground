# Current work: the dense map across the volume

<div class="flex justify-center mt-2">
  <video src="/videos/dense-scroll.mp4" autoplay loop muted playsinline controls
         style="max-height: 330px; max-width: 92%; object-fit: contain;"></video>
</div>

<div class="mt-3 text-center opacity-80">The same per-slice merge, scrolled through all 336 worked slices (z 1293 to 1628). One colour per neuron, held across the stack.</div>

<!--
Opening: same dense map, now scrolled through the whole worked volume

- 336 slices, z 1293 to 1628, the full band we have run
- one colour per neuron, stable across the stack, so you can watch a cell persist in depth
- controls are on: I can pause on any slice
- if the video stutters, the previous slide is the same map as a still

Timing: 45 seconds
-->
