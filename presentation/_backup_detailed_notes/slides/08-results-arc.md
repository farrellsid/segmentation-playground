# Bleed down about 5x, coverage near total

<div class="flex justify-center mt-3">
  <img src="/images/progress-arc.png" class="max-h-[50vh] max-w-[86%] object-contain" />
</div>

<div class="mt-2 text-center opacity-80">Per-slice beats propagation. SAM3 beats SAM2 on bleed, at about 3 to 4x the compute.</div>

<!--
Opening: "This is the journey, not a single number."

Beats: across the pipeline changes, the bleed rate fell from about 0.45 to about 0.09, and coverage of a neuron's own node rose from about two thirds to essentially always. Of the three methods, per-slice beats propagation. And the newer SAM3 beats SAM2 on bleed, though it is 3 to 4 times slower per cell, which is an honest cost. Exact 4-way numbers are in the backup.

Timing: 2 minutes
-->
